{
  "project_type": "Google Colab notebook script (Python .py file representing a Colab notebook)",
  "language": "Python",
  "build_system": "None - single-file script intended to be run in Google Colab",
  "test_framework": "None - manual validation only (script runs in Colab, not testable locally)",
  "build_command": "python -c \"import ast; ast.parse(open('ltx23_director_mv_30s.py').read()); print('Syntax OK')\"",
  "test_command": "python -c \"import ast; ast.parse(open('ltx23_director_mv_30s.py').read()); print('Syntax OK')\"",
  "verification_instructions": "1. Run Python AST parse to confirm no syntax errors. 2. Verify the file follows the same six-cell structure as ltx2_ti2v_distilled.py. 3. Verify all node IDs and widget values from the JSON workflow are correctly translated.",
  "snapshot_or_generated_files": null,
  "setup_instructions": "No setup needed - this is a file generation task",
  "environment_constraints": "Internet access available. No Colab runtime available for actual execution testing - use AST validation only.",
  "contribution_requirements": "Output must be a single .py file at /projects/sandbox/ms/ltx23_director_mv_30s.py matching the style of ltx2_ti2v_distilled.py",
  "key_patterns": "Reference script conventions: (1) NODE_CLASS_MAPPINGS['NodeName']() instantiation pattern, (2) .EXECUTE_NORMALIZED() method calls with keyword args matching widget names, (3) get_value_at_index() to extract outputs, (4) @param Colab form annotations as inline comments, (5) del + torch.cuda.empty_cache() + gc.collect() after heavy models, (6) with torch.inference_mode() wrapper, (7) model_download() helper with aria2c, (8) Six-cell structure: environment prep, helpers, model downloads, media uploads, main generation function, param form + invocation",
  "relevant_files": [
    "/projects/sandbox/ms/ltx2_ti2v_distilled.py",
    "/projects/sandbox/ms/LTX-2.3_Director_2.0-MV-Workflow-30s.json"
  ],
  "directory_structure": "Flat repo root with 2 source files. Output file goes alongside them at repo root."
}
