{
  "id": "FEAT-004",
  "type": "fix",
  "description": "Fix upload helper functions to write file bytes directly from the uploaded dict instead of using shutil.move() with hardcoded paths that fail when Colab renames files or when the current working directory differs from /content/ComfyUI/",
  "status": "in_progress",
  "steps": [
    "Fix upload_file() to write uploaded[filename] bytes to dest_path directly",
    "Fix upload_image() to write uploaded[filename] bytes to dest_path directly",
    "Fix upload_multiple_images() to write uploaded[filename] bytes to dest_path directly",
    "Fix upload_audio() to write uploaded[filename] bytes to dest_path directly",
    "Remove unused shutil import if no longer needed elsewhere"
  ],
  "acceptance_criteria": [
    "No function uses shutil.move() for uploaded file handling",
    "All four functions write bytes from the uploaded dict directly to the destination path",
    "File still passes AST syntax validation"
  ],
  "verification": [
    "python3 -c \"import ast; content = open('ltx23_director_mv_30s.py').read(); lines = content.split('\\n'); clean = '\\n'.join(['# '+l if l.strip().startswith('!') or l.strip().startswith('# %cd') else l for l in lines]); ast.parse(clean); print('OK')\""
  ],
  "blocked_reason": null,
  "findings": null
}
